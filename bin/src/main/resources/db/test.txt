CREATE TABLE `NOTICE` (
	`NOTICE_NO`	INT	NOT NULL,
	`NOTICE_TITLE`	VARCHAR(40)	NOT NULL,
	`NOTICE_CONTENT`	VARCHAR(400)	NOT NULL,
	`NOTICE_WRITEDATE`	DATE	NOT NULL,
	`NOTICE_WRITER`	VARCHAR(40)	NOT NULL,
	`NOTICE_VIEW`	INT	NULL
);

CREATE TABLE `QNA` (
	`QNA_NO`	INT	NOT NULL,
	`USER_ID`	VARCHAR(100)	NOT NULL,
	`QNA_TYPE`	VARCHAR(200)	NOT NULL,
	`QNA_TITLE`	VARCHAR(100)	NOT NULL,
	`QNA_DATE`	DATE	NOT NULL,
	`QNA_CONTENT`	VARCHAR(400)	NOT NULL
);

CREATE TABLE `PAYMENT` (
	`ORDERNO`	VARCHAR(100)	NOT NULL,
	`USER_ID`	VARCHAR(100)	NOT NULL,
	`TEMPLATE_NO`	INT	NOT NULL,
	`OPTION_NO`	INT	NOT NULL,
	`RECEIVER`	VARCHAR(100)	NOT NULL,
	`EMAIL`	VARCHAR(100)	NOT NULL,
	`ADDR`	VARCHAR(200)	NOT NULL,
	`PHONE`	VARCHAR(100)	NOT NULL,
	`DELIVERYDATE`	DATE	NULL,
	`POINT`	INT	NULL
);

CREATE TABLE `CART` (
	`CART_NO`	INT	NOT NULL,
	`USER_ID`	VARCHAR(100)	NOT NULL,
	`TEMPLATE_NO`	INT	NOT NULL,
	`OPTION_NO`	INT	NOT NULL,
	`CART_AMOUNT`	INT	NOT NULL,
	`PRODUCT_QUANTITY`	INT	NOT NULL,
	`CART_STATUS`	VARCHAR(1)	NOT NULL,
	`DISCOUNT_`	INT	NULL,
	`PRODUCT_COST`	INT	NULL
);

CREATE TABLE `DIARY` (
	`DIARY_NO`	INT	NOT NULL,
	`USER_ID`	VARCHAR(100)	NOT NULL,
	`KIDS_NO`	INT	NOT NULL,
	`DIARY_CONTENT`	VARCHAR(200)	NOT NULL,
	`WRITE_DATE`	VARCHAR(40)	NOT NULL,
	`PHONE`	VARCHAR(100)	NOT NULL,
	`WRITE_COUNT`	INT	NOT NULL
);

CREATE TABLE `REVIEW` (
	`REVIEW_NO`	INT	NOT NULL,
	`USER_ID`	VARCHAR(100)	NOT NULL,
	`REVIEW_TITLE`	VARCHAR(200)	NOT NULL,
	`REVIEW_EVALUATION`	VARCHAR(100)	NOT NULL,
	`REVIEW_BEST`	INT	NOT NULL,
	`REVIEW_CONTENT`	VARCHAR(1000)	NOT NULL,
	`REVIEW_COUNT`	INT	NOT NULL,
	`REVIEW_DATE`	DATE	NOT NULL,
	`RIMG_NAME`	VARCHAR(200)	NULL,
	`RIMG_PATH`	VARCHAR(300)	NULL
);

CREATE TABLE `STICKER` (
	`DIARY_NO`	INT	NOT NULL,
	`STICKER_COUNT`	INT	NULL
);

CREATE TABLE `POINT` (
	`ORDERNO`	VARCHAR(100)	NOT NULL,
	`USERID`	VARCHAR(100)	NOT NULL,
	`POINT_SAVE`	INT	NOT NULL,
	`POINT_DATE`	DATE	NOT NULL,
	`POINT_CONTENT`	VARCHAR(200)	NOT NULL,
	`POINT_USE`	INT	NOT NULL,
	`POINT_ALL`	INT	NOT NULL,
	`POINT_PURPOSE`	VARCHAR(20)	NOT NULL
);

CREATE TABLE `MEMBER` (
	`USER_ID`	VARCHAR(100)	NOT NULL,
	`PASSWORD`	VARCHAR(100)	NOT NULL,
	`NAME`	VARCHAR(100)	NOT NULL,
	`POST_CODE`	VARCHAR(10)	NOT NULL,
	`DEFAULT_ADDR`	VARCHAR(200)	NOT NULL,
	`DETAIL _ADDR`	VARCHAR(200)	NOT NULL,
	`PHONE`	VARCHAR(100)	NOT NULL,
	`EMAIL`	VARCHAR(100)	NOT NULL
);

SELECT *FROM MEMBER;



CREATE TABLE `NEWYEAR` (
	`NY_NO`	INT	NOT NULL,
	`USER_ID`	VARCHAR(100)	NOT NULL,
	`NY_DATE`	DATE	NOT NULL,
	`NY_SENDER`	VARCHAR(50)	NOT NULL,
	`NY_RECEIVER`	VARCHAR(200)	NOT NULL,
	`NY_CONTENT`	VARCHAR(1000)	NOT NULL
);

CREATE TABLE `NYIMG` (
	`NY_NO`	INT	NOT NULL,
	`NYIMG_NAME`	VARCHAR(200)	NOT NULL,
	`NYIMG_PATH`	VARCHAR(300)	NOT NULL
);

CREATE TABLE `TREE` (
	`TREE_NO`	INT	NOT NULL,
	`USER_ID`	VARCHAR(100)	NOT NULL,
	`TIMG_PATH`	VARCHAR(300)	NOT NULL,
	`TIMG_NAME`	VARCHAR(200)	NOT NULL
);

CREATE TABLE `KIDS_SETTING` (
	`KIDS_NO`	INT	NOT NULL,
	`USER_ID`	VARCHAR(100)	NOT NULL,
	`KIDS_POINT`	INT	NULL,
	`KIDS_NICKNAME`	VARCHAR(100)	NULL,
	`KIDS_THUMBNAIL`	VARCHAR(200)	NULL,
	`KIDS_LETTER_IMG`	VARCHAR(200)	NULL
);

CREATE TABLE `QNA_FILE` (
	`QNA_NO`	INT	NOT NULL,
	`FILE_ORIGIN_NAME`	VARCHAR(100)	NOT NULL,
	`FILE_PATH`	VARCHAR(100)	NULL
);

CREATE TABLE `ORDER` (
	`ORDERNO`	VARCHAR(100)	NOT NULL,
	`USER_ID`	VARCHAR(100)	NOT NULL,
	`ORDER_DATE`	DATE	NOT NULL,
	`REVIEW_STATUS`	VARCHAR(1)	NOT NULL,
	`DELIVERY_STATUS`	VARCHAR(1)	NOT NULL,
	`PRODUCT_QUANTITY`	INT	NULL,
	`PRODUCT_COST`	INT	NULL
);

CREATE TABLE `KIDS_FILE` (
	`KIDS_NO`	INT	NOT NULL,
	`FILE_PATH`	VARCHAR(200)	NOT NULL,
	`FILE_ORIGIN_NAME`	VARCHAR(100)	NOT NULL
);

CREATE TABLE `ORNAMENT` (
	`TREE_NO`	INT	NOT NULL,
	`OR_NO`	INT	NOT NULL,
	`OR_PRICE`	INT	NOT NULL
);

CREATE TABLE `OPTION` (
	`OPTION_NO`	INT	NOT NULL,
	`OPTION_NAME`	VARCHAR(100)	NOT NULL,
	`OPTION_PRICE`	INT	NOT NULL,
	`OPTION_IMG`	VARCHAR(300)	NOT NULL,
	`PS_MENT`	VARCHAR(200)	NULL
);

CREATE TABLE `TEMPLATE` (
	`TEMPLATE_NO`	INT	NOT NULL,
	`TEMPLATE_NAME`	VARCHAR(100)	NOT NULL,
	`TEMPLATE_PRICE`	INT	NOT NULL,
	`TEMPLATE_IMG`	VARCHAR(300)	NOT NULL,
	`BLANK1`	VARCHAR(100)	NOT NULL,
	`BLANK2`	VARCHAR(100)	NOT NULL,
	`BLANK3`	VARCHAR(100)	NOT NULL,
	`PS`	VARCHAR(200)	NULL
);

CREATE TABLE `DELIVERY` (
	`ORDERNO`	VARCHAR(100)	NOT NULL,
	`INVOICENUM`	VARCHAR(20)	NOT NULL
);

ALTER TABLE `NOTICE` ADD CONSTRAINT `PK_NOTICE` PRIMARY KEY (
	`NOTICE_NO`
);

ALTER TABLE `QNA` ADD CONSTRAINT `PK_QNA` PRIMARY KEY (
	`QNA_NO`
);

ALTER TABLE `PAYMENT` ADD CONSTRAINT `PK_PAYMENT` PRIMARY KEY (
	`ORDERNO`
);

ALTER TABLE `CART` ADD CONSTRAINT `PK_CART` PRIMARY KEY (
	`CART_NO`
);

ALTER TABLE `DIARY` ADD CONSTRAINT `PK_DIARY` PRIMARY KEY (
	`DIARY_NO`
);

ALTER TABLE `REVIEW` ADD CONSTRAINT `PK_REVIEW` PRIMARY KEY (
	`REVIEW_NO`
);

ALTER TABLE `STICKER` ADD CONSTRAINT `PK_STICKER` PRIMARY KEY (
	`DIARY_NO`
);

ALTER TABLE `POINT` ADD CONSTRAINT `PK_POINT` PRIMARY KEY (
	`ORDERNO`
);

ALTER TABLE `MEMBER` ADD CONSTRAINT `PK_MEMBER` PRIMARY KEY (
	`USER_ID`
);

ALTER TABLE `NEWYEAR` ADD CONSTRAINT `PK_NEWYEAR` PRIMARY KEY (
	`NY_NO`
);

ALTER TABLE `NYIMG` ADD CONSTRAINT `PK_NYIMG` PRIMARY KEY (
	`NY_NO`
);

ALTER TABLE `TREE` ADD CONSTRAINT `PK_TREE` PRIMARY KEY (
	`TREE_NO`
);

ALTER TABLE `KIDS_SETTING` ADD CONSTRAINT `PK_KIDS_SETTING` PRIMARY KEY (
	`KIDS_NO`
);

ALTER TABLE `ORDER` ADD CONSTRAINT `PK_ORDER` PRIMARY KEY (
	`ORDERNO`
);

ALTER TABLE `ORNAMENT` ADD CONSTRAINT `PK_ORNAMENT` PRIMARY KEY (
	`TREE_NO`
);

ALTER TABLE `OPTION` ADD CONSTRAINT `PK_OPTION` PRIMARY KEY (
	`OPTION_NO`
);

ALTER TABLE `TEMPLATE` ADD CONSTRAINT `PK_TEMPLATE` PRIMARY KEY (
	`TEMPLATE_NO`
);

ALTER TABLE `DELIVERY` ADD CONSTRAINT `PK_DELIVERY` PRIMARY KEY (
	`ORDERNO`
);

ALTER TABLE `STICKER` ADD CONSTRAINT `FK_DIARY_TO_STICKER_1` FOREIGN KEY (
	`DIARY_NO`
)
REFERENCES `DIARY` (
	`DIARY_NO`
);

ALTER TABLE `POINT` ADD CONSTRAINT `FK_PAYMENT_TO_POINT_1` FOREIGN KEY (
	`ORDERNO`
)
REFERENCES `PAYMENT` (
	`ORDERNO`
);

ALTER TABLE `NYIMG` ADD CONSTRAINT `FK_NEWYEAR_TO_NYIMG_1` FOREIGN KEY (
	`NY_NO`
)
REFERENCES `NEWYEAR` (
	`NY_NO`
);

ALTER TABLE `ORDER` ADD CONSTRAINT `FK_PAYMENT_TO_ORDER_1` FOREIGN KEY (
	`ORDERNO`
)
REFERENCES `PAYMENT` (
	`ORDERNO`
);

ALTER TABLE `ORNAMENT` ADD CONSTRAINT `FK_TREE_TO_ORNAMENT_1` FOREIGN KEY (
	`TREE_NO`
)
REFERENCES `TREE` (
	`TREE_NO`
);

ALTER TABLE `DELIVERY` ADD CONSTRAINT `FK_PAYMENT_TO_DELIVERY_1` FOREIGN KEY (
	`ORDERNO`
)
REFERENCES `PAYMENT` (
	`ORDERNO`
);

